<section id="hero" class="hero">
  <div class="decorative-elements">
    <div class="circle circle-1"></div>
    <div class="circle circle-2"></div>
    <div class="circle circle-3"></div>
    <div class="triangle triangle-1"></div>
    <div class="triangle triangle-2"></div>
    <div class="square square-1"></div>
  </div>
  <div class="container">
    <div class="hero-content">
      <div class="hero-logo">
        <img src="/assets/logo_nospace.svg" alt="脳活ラボ" loading="lazy">
      </div>
      <div class="hero-text">
        <h1 class="hero-title">元気に楽しく脳活！</h1>
        <p class="hero-tagline">介護予防・認知症予防をもっと身近に。</p>
        <p class="hero-description">
          「脳活ラボ」は、自治体 と連携できるLINEアプリです。
        </p>
        <div class="hero-buttons">
          <a href="#download" class="button">はじめる</a>
          <a href="#features" class="button button-outline">詳しく見る</a>
        </div>
      </div>
    </div>
  </div>
</section>

<style>
  .hero {
    position: relative;
    min-height: 500px;
    display: flex;
    align-items: center;
    justify-content: center;
    color: var(--color-sumi);
    margin-top: 0;
    padding: 5rem 0;
    overflow: hidden;
    background-color: #ffffff;
  }

  /* Decorative elements */
  .decorative-elements {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    z-index: 0;
    overflow: hidden;
    pointer-events: none;
  }

  .circle {
    position: absolute;
    border-radius: 50%;
    opacity: 0.1;
  }

  .circle-1 {
    top: 15%;
    left: 8%;
    width: 100px;
    height: 100px;
    background-color: var(--color-indigo);
    animation: float 8s ease-in-out infinite;
  }

  .circle-2 {
    bottom: 20%;
    right: 12%;
    width: 150px;
    height: 150px;
    background-color: var(--color-gold);
    animation: float 9s ease-in-out infinite reverse;
  }

  .circle-3 {
    top: 65%;
    left: 65%;
    width: 50px;
    height: 50px;
    background-color: #367770; /* Middle gradient color */
    animation: float 7s ease-in-out infinite;
  }

  .triangle {
    position: absolute;
    width: 0;
    height: 0;
    opacity: 0.1;
  }

  .triangle-1 {
    top: 25%;
    right: 25%;
    border-left: 40px solid transparent;
    border-right: 40px solid transparent;
    border-bottom: 70px solid var(--color-indigo);
    transform: rotate(15deg);
    animation: float 10s ease-in-out infinite;
  }

  .triangle-2 {
    bottom: 30%;
    left: 18%;
    border-left: 25px solid transparent;
    border-right: 25px solid transparent;
    border-bottom: 45px solid var(--color-gold);
    transform: rotate(-20deg);
    animation: float 12s ease-in-out infinite reverse;
  }

  .square {
    position: absolute;
    opacity: 0.08;
  }

  .square-1 {
    top: 45%;
    right: 8%;
    width: 70px;
    height: 70px;
    background-color: #367770;
    transform: rotate(30deg);
    animation: float 11s ease-in-out infinite;
  }

  @keyframes float {
    0% {
      transform: translateY(0) rotate(0);
    }
    50% {
      transform: translateY(-15px) rotate(5deg);
    }
    100% {
      transform: translateY(0) rotate(0);
    }
  }

  .hero-content {
    position: relative;
    z-index: 1;
    width: 100%;
    max-width: 100%;
    padding: 1.5rem;
    margin: 0 auto;
    display: flex;
    flex-direction: column;
    align-items: center;
    text-align: center;
  }

  .hero-logo {
    margin-bottom: 2rem;
  }

  .hero-logo img {
    width: 180px;
    height: auto;
  }

  .hero-text {
    width: 100%;
    max-width: 600px;
    text-align: center;
  }

  .hero-title {
    font-size: 2.2rem;
    line-height: 1.3;
    margin-bottom: 1rem;
    color: var(--color-indigo);
    font-weight: 700;
  }

  .sp-only {
    display: inline;
  }

  .hero-tagline {
    font-size: 1.3rem;
    margin-bottom: 1rem;
    color: var(--color-gold);
    font-weight: 600;
  }

  .hero-description {
    font-size: 1rem;
    margin-bottom: 2rem;
    color: var(--color-sumi);
    word-break: keep-all;
    overflow-wrap: break-word;
  }

  .hero-buttons {
    display: flex;
    flex-direction: column;
    gap: 1rem;
    width: 100%;
    margin-top: 1.5rem;
  }

  .button-outline {
    background-color: transparent;
    border: 2px solid var(--color-indigo);
    color: var(--color-indigo);
  }

  .button-outline:hover {
    background-color: var(--color-indigo);
    color: white;
  }

  @media (min-width: 481px) {
    .hero-title {
      font-size: 2.8rem;
    }

    .hero-logo img {
      width: 220px;
    }

    .hero-buttons {
      flex-direction: row;
      justify-content: center;
    }
  }

  @media (min-width: 769px) {
    .sp-only {
      display: none;
    }

    .hero {
      min-height: 600px;
      padding: 6rem 0;
    }

    .hero-content {
      padding: 2rem;
    }

    .hero-logo img {
      width: 250px;
    }

    .hero-title {
      font-size: 3.5rem;
      margin-bottom: 1.2rem;
    }

    .hero-tagline {
      font-size: 1.6rem;
      margin-bottom: 1.5rem;
    }

    .hero-description {
      font-size: 1.2rem;
      margin-bottom: 2rem;
    }

    .hero-buttons .button {
      width: auto;
      padding: 0.8rem 2rem;
    }

    /* Larger decorative elements on desktop */
    .circle-1 {
      width: 120px;
      height: 120px;
    }

    .circle-2 {
      width: 180px;
      height: 180px;
    }

    .triangle-1 {
      border-left: 70px solid transparent;
      border-right: 70px solid transparent;
      border-bottom: 120px solid var(--color-indigo);
    }
  }
</style>
