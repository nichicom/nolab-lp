---
const steps = [
  {
    title: "脳活ラボLINE公式を友だち追加",
    description: "友だち追加するだけでアカウント登録が完了します。",
    icon: "📱"
  },
  {
    title: "プロフィール入力",
    description: "LINEメッセージでプロフィール入力のリンクが送付されるため、入力します。",
    icon: "✍️"
  },
  {
    title: "予防プログラムを始める",
    description: "多彩なメニューから好きなものを選んで毎日続けましょう。",
    icon: "🧠"
  },
  {
    title: "ポイントを貯める",
    description: "トレーニング実績に応じてポイントが貯まります。継続するほど効果的です。",
    icon: "🎯"
  },
  {
    title: "ポイントを活用",
    description: "貯めたポイントは、アプリ内のアイテムやギフト券が当たる抽選券に交換できます。",
    icon: "🎁"
  }
];
---

<section id="howto" class="howto">
  <div class="container">
    <h2 class="section-title">
      <span class="title-decoration">使い方</span>
    </h2>
    <div class="steps-container">
      {steps.map((step, index) => (
        <div class="step-card">
          <div class="step-number">{index + 1}</div>
          <div class="step-icon">{step.icon}</div>
          <h3>{step.title}</h3>
          <p>{step.description}</p>
        </div>
      ))}
    </div>
  </div>
</section>

<style>
  .howto {
    background-color: white;
    position: relative;
  }

  .section-title {
    text-align: center;
    margin-bottom: 2rem;
    font-size: 1.8rem;
  }

  .title-decoration {
    position: relative;
    display: inline-block;
  }

  .title-decoration::after {
    content: "";
    position: absolute;
    bottom: -8px;
    left: 0;
    width: 100%;
    height: 3px;
    background: linear-gradient(90deg, var(--color-indigo), #367770, var(--color-gold));
  }

  .steps-container {
    display: flex;
    flex-direction: column;
    gap: 2rem;
    max-width: 800px;
    margin: 0 auto;
  }

  .step-card {
    display: flex;
    flex-direction: column;
    align-items: center;
    text-align: center;
    padding: 1.5rem;
    background-color: #f8f9fa;
    border-radius: 10px;
    position: relative;
    box-shadow: 0 5px 15px rgba(0, 0, 0, 0.05);
  }

  .step-number {
    position: absolute;
    top: -15px;
    left: 50%;
    transform: translateX(-50%);
    background-color: var(--color-indigo);
    color: white;
    width: 30px;
    height: 30px;
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    font-weight: bold;
    font-size: 1rem;
  }

  .step-icon {
    font-size: 2.5rem;
    margin-bottom: 1rem;
    color: var(--color-gold);
  }

  .step-card h3 {
    color: var(--color-indigo);
    margin-bottom: 0.8rem;
    font-size: 1.3rem;
  }

  .step-card p {
    color: var(--color-sumi);
    line-height: 1.6;
    font-size: 0.95rem;
  }

  @media (min-width: 769px) {
    .section-title {
      margin-bottom: 3rem;
      font-size: 2.2rem;
    }

    .title-decoration::after {
      bottom: -10px;
    }

    .steps-container {
      flex-direction: row;
      justify-content: space-between;
      gap: 1.5rem;
      max-width: 100%;
    }

    .step-card {
      flex: 1;
      padding: 2rem 1rem;
    }

    .step-icon {
      font-size: 3rem;
    }

    .step-card h3 {
      font-size: 1.4rem;
    }
  }
</style>
