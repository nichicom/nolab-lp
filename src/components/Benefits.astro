---
const benefits = [
  {
    title: "健康維持",
    description: "毎日の脳トレで認知機能を維持し、健康的な生活をサポートします。",
    icon: "🧠"
  },
  {
    title: "コミュニティ形成",
    description: "地域の人々とつながり、活気あるコミュニティを形成します。",
    icon: "👥"
  },
  {
    title: "地域経済活性化",
    description: "ポイント還元システムで地域の店舗や施設の利用を促進します。",
    icon: "💰"
  },
  {
    title: "データ活用",
    description: "匿名化されたデータを活用し、地域の健康増進施策に役立てます。",
    icon: "📊"
  }
];
---

<section id="benefits" class="benefits">
  <div class="container">
    <h2 class="section-title">
      <span class="title-decoration">脳活ラボの効果</span>
    </h2>
    <div class="benefits-grid">
      {benefits.map((benefit, index) => (
        <div class="benefit-card">
          <div class="benefit-icon">{benefit.icon}</div>
          <h3>{benefit.title}</h3>
          <p>{benefit.description}</p>
        </div>
      ))}
    </div>
  </div>
</section>

<style>
  .benefits {
    background-color: #f8f9fa;
    position: relative;
  }

  .section-title {
    text-align: center;
    margin-bottom: 2rem;
    font-size: 1.8rem;
  }

  .title-decoration {
    position: relative;
    display: inline-block;
  }

  .title-decoration::after {
    content: "";
    position: absolute;
    bottom: -8px;
    left: 0;
    width: 100%;
    height: 3px;
    background: linear-gradient(90deg, var(--color-indigo), #367770, var(--color-gold));
  }

  .benefits-grid {
    display: grid;
    grid-template-columns: 1fr;
    gap: 1.5rem;
  }

  .benefit-card {
    background-color: white;
    border-radius: 10px;
    padding: 1.5rem;
    box-shadow: 0 5px 15px rgba(0, 0, 0, 0.05);
    transition: transform 0.3s ease, box-shadow 0.3s ease;
    position: relative;
    overflow: hidden;
    z-index: 1;
    text-align: center;
  }

  .benefit-card::before {
    content: "";
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background: linear-gradient(135deg, rgba(230, 149, 0, 0.05), rgba(10, 30, 60, 0.05));
    z-index: -1;
  }

  .benefit-card:hover {
    transform: translateY(-5px);
    box-shadow: 0 10px 25px rgba(0, 0, 0, 0.1);
  }

  .benefit-icon {
    font-size: 3rem;
    margin-bottom: 1.2rem;
    color: var(--color-gold);
  }

  .benefit-card h3 {
    color: var(--color-indigo);
    margin-bottom: 0.8rem;
    font-size: 1.3rem;
  }

  .benefit-card p {
    color: var(--color-sumi);
    line-height: 1.6;
    font-size: 0.95rem;
  }

  @media (min-width: 601px) {
    .benefits-grid {
      grid-template-columns: repeat(2, 1fr);
      gap: 1.5rem;
    }

    .benefit-card h3 {
      font-size: 1.4rem;
    }
  }

  @media (min-width: 901px) {
    .section-title {
      margin-bottom: 3rem;
      font-size: 2.2rem;
    }

    .title-decoration::after {
      bottom: -10px;
    }

    .benefits-grid {
      grid-template-columns: repeat(4, 1fr);
      gap: 2rem;
    }

    .benefit-card {
      padding: 2rem;
    }

    .benefit-icon {
      font-size: 3.5rem;
      margin-bottom: 1.5rem;
    }

    .benefit-card h3 {
      margin-bottom: 1rem;
      font-size: 1.5rem;
    }

    .benefit-card p {
      line-height: 1.7;
      font-size: 1rem;
    }
  }
</style>
